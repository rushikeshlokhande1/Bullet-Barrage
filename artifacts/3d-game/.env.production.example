# Public HTTPS origin of the deployed Socket.IO/API server.
# CrazyGames hosts the client as static files, so multiplayer cannot use window.location.origin.
VITE_API_ORIGIN=https://your-bullet-barrage-api.example.com

# Keep relative paths for CrazyGames ZIP uploads.
BASE_PATH=./
